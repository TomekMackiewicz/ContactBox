{% extends '::base.html.twig' %}

{% block body %}

	{{ form_start(form) }}
		{{ form_label(form.fullAddress) }}
		{{ form_widget(form.fullAddress, {'attr':{'class':'form-control'}}) }}
		<br>
		{{ form_widget(form.update, {'attr':{'class':'btn btn-default'}}) }}
	{{ form_end(form) }}

{% endblock %}