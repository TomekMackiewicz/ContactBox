{% extends '::base.html.twig' %}

{% block body %}

	{{ form_start(form) }}
		{{ form_label(form.fullAddress) }}
		{{ form_widget(form.fullAddress, {'attr':{'class':'form-control','placeholder':'Address...'}}) }}
		{{ form_widget(form.contact_id, {value:app.request.query.get("id"),'attr':{'class':'hidden'}}) }}
		<br>
		{{ form_widget(form.save, {'attr':{'class':'btn btn-default'}}) }}
	{{ form_end(form) }}

{% endblock %}